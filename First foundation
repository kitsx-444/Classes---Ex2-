# I am calling rating incorrectly because self.rating is now overwriting the method

class Currency():
	def __init__(self, code, score, trend):
		self.code = code
		self.score = int(score)
		self.trend = trend
	def rating(self):
		if self.score >= 75:
			self.rating = 'Strong'
		elif self.score <= 74 and self.score >= 50:
			self.rating = 'Moderate'
		else:
			self.rating = 'Weak'
	def display(self):
		print(f'Currency: {self.code}')
		print(f'Currency: {self.score}')
		print(f'Trend: {self.trend}')
		print(f'Rating: {self.rating}')

currency1 = Currency('USD', 42, 'Weak')
currency2 = Currency('EUR', 55, 'Neutral')
currency3 = Currency('GBP', 2, 'Weak')
